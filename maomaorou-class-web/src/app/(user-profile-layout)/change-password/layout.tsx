import { Metadata } from "next";

export const metadata: Metadata = {
  title: "變更密碼 - 貓貓肉線上課程網站",
  description: "貓貓肉線上課程網站變更密碼",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
